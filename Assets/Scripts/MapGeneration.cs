using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGeneration : MonoBehaviour
{

    [Header("MAP TYPE")]
    public bool randomSections = true;
    public bool redSection = false;
    public bool blueSection = false;
    public bool greenSection = false;
    public bool yellowSection = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
